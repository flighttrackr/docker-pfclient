{
    "tcp_address": "{{TCP_HOST}}",
    "tcp_port": "{{TCP_PORT}}",
    "select_timeout": "10",
    "data_upload_interval": "10",
    "connection_type": "{{CONNECTION_TYPE}}",
    "aircraft_timeout": "30",
    "data_format": "1",
    "latitude": "{{LAT}}",
    "longitude": "{{LON}}",
    "sharecode": "{{SHARING_KEY}}"
}
